# دليل تسجيل دخول المشرف/المعلم 👨‍🏫

## معلومات حساب المشرف

**رقم الموبايل:** `01228495250`  
**كلمة المرور:** `y2081049`  
**الدور:** معلم (Teacher)

---

## 🔧 خطوات إنشاء حساب المشرف لأول مرة

### الطريقة 1: من خلال Console المتصفح (موصى بها)

1. **افتح الموقع** في المتصفح
2. **افتح Developer Console**:
   - Windows/Linux: اضغط `F12` أو `Ctrl + Shift + J`
   - Mac: اضغط `Cmd + Option + J`
3. **اكتب الأمر التالي** في الكونسول:
   ```javascript
   createAdminAccount()
   ```
4. **اضغط Enter** وانتظر الرسالة
5. ستظهر رسالة تأكيد عند نجاح العملية

### الطريقة 2: التحقق من وجود الحساب

إذا كان الحساب موجوداً بالفعل، ستظهر رسالة تخبرك بذلك.

---

## 🚪 تسجيل الدخول كمشرف/معلم

### الخطوة 1: الذهاب لصفحة تسجيل دخول المعلم

اذهب إلى: **`/teacher-login`**

أو اكتب في شريط العنوان:
```
http://localhost:5173/teacher-login
```

### الخطوة 2: أدخل البيانات

- **رقم الموبايل:** `01228495250`
- **كلمة المرور:** `y2081049`

### الخطوة 3: اضغط "دخول"

سيتم توجيهك تلقائياً إلى لوحة تحكم المعلم (`/dashboard`)

---

## ❌ حل المشاكل الشائعة

### المشكلة 1: خطأ 400 (Bad Request)

**السبب:** محاولات تسجيل دخول متكررة أو بيانات خاطئة

**الحل:**
1. تأكد من رقم الموبايل وكلمة المرور
2. انتظر دقيقة واحدة قبل المحاولة مرة أخرى
3. امسح الكاش والكوكيز
4. جرب في نافذة تصفح خفي (Incognito)

### المشكلة 2: "auth/too-many-requests"

**السبب:** محاولات تسجيل دخول كثيرة جداً

**الحل:**
1. انتظر 15-60 دقيقة
2. جرب من شبكة إنترنت مختلفة
3. استخدم VPN لتغيير IP

### المشكلة 3: "auth/user-not-found"

**السبب:** الحساب غير موجود

**الحل:**
1. افتح Console المتصفح
2. نفذ: `createAdminAccount()`
3. انتظر رسالة النجاح
4. حاول تسجيل الدخول مرة أخرى

### المشكلة 4: "auth/wrong-password"

**السبب:** كلمة المرور خاطئة

**الحل:**
1. تأكد من كلمة المرور: `y2081049`
2. تأكد من عدم وجود مسافات إضافية
3. جرب نسخ ولصق كلمة المرور

---

## 🔍 التحقق من حالة الحساب

### من خلال Console:

```javascript
// التحقق من المستخدم الحالي
console.log(auth.currentUser);

// التحقق من البروفايل
const docRef = doc(db, 'profiles', 'USER_ID_HERE');
const docSnap = await getDoc(docRef);
console.log(docSnap.data());
```

---

## 📝 ملاحظات مهمة

### ✅ ما تم إصلاحه:

1. **إزالة التسجيل التلقائي** - لن يحاول التطبيق تسجيل الدخول تلقائياً عند كل تحميل
2. **إضافة صفحة تسجيل دخول منفصلة** - `/teacher-login` للمعلمين
3. **تحسين معالجة الأخطاء** - رسائل خطأ واضحة بالعربية
4. **إنشاء يدوي للحساب** - من خلال Console لتجنب المشاكل

### ⚠️ تحذيرات:

- **لا تشارك** بيانات تسجيل الدخول مع أحد
- **غير كلمة المرور** بعد أول تسجيل دخول (عند إضافة هذه الميزة)
- **لا تنشر** API Keys على GitHub

---

## 🎯 الخطوات التالية الموصى بها

### 1. إضافة ميزة تغيير كلمة المرور
```typescript
import { updatePassword } from 'firebase/auth';

async function changePassword(newPassword: string) {
  const user = auth.currentUser;
  if (user) {
    await updatePassword(user, newPassword);
  }
}
```

### 2. إضافة ميزة استعادة كلمة المرور
```typescript
import { sendPasswordResetEmail } from 'firebase/auth';

async function resetPassword(email: string) {
  await sendPasswordResetEmail(auth, email);
}
```

### 3. إضافة Two-Factor Authentication (2FA)
- استخدم Firebase Phone Authentication
- أضف رمز تحقق عبر SMS

### 4. تسجيل محاولات تسجيل الدخول
- احفظ سجل بمحاولات تسجيل الدخول
- راقب الأنشطة المشبوهة

---

## 📞 الدعم

إذا واجهت أي مشاكل:

1. **تحقق من Console** - ابحث عن رسائل الخطأ
2. **راجع الوثائق** - اقرأ هذا الملف بعناية
3. **امسح البيانات** - امسح الكاش والكوكيز
4. **أعد تشغيل الخادم** - أوقف وأعد تشغيل `npm run dev`

---

## ✨ نصائح للأمان

1. ✅ استخدم كلمات مرور قوية (8+ أحرف، أرقام، رموز)
2. ✅ فعّل HTTPS في الإنتاج
3. ✅ لا تحفظ كلمات المرور في الكود
4. ✅ استخدم متغيرات البيئة (.env)
5. ✅ راجع Firebase Security Rules بانتظام

---

**آخر تحديث:** 2025-12-22  
**الإصدار:** 1.0
